// Operações Bitwise - bit a bit

let n1 = 20;
let n2 = 14;

let resDeslocamento = n1 << 1; // Para a esquerda, multiplica por 2, ao deslocar para a esquerda dobra
let resDeslocamento2 = n1 >> 1; // Para a direita, divide por 2, ao deslocar para a direita, retorna a metade
console.log(resDeslocamento);
