// Set - Não permite entrada de dados iguais, dados duplicados

const caixa = document.querySelector("#caixa");

const curso = "JavaScript";
const nome = "Kaue";
const frase = `Eu sou o ${nome} e estou me aprofundando em ${curso}`;

caixa.innerHTML = frase;
